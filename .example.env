# ============================================
# Trivy Dashboard - Environment Configuration
# ============================================

# Port Configuration
BACKEND_PORT=8181                    # Backend port (host'ta dinleyeceği port, container içinde 8080)
FRONTEND_PORT=80                   # Frontend port (host'ta dinleyeceği port, container içinde 80)

# Directory Configuration
EXPORT_DIR=./export                  # Trivy JSON raporlarının bulunduğu klasör (host path)

# Frontend API Configuration
VITE_API_BASE=http://localhost:8181  # Frontend'in backend API'sine erişmek için kullanacağı URL (ÖNEMLİ: Sonunda /api EKLEMEYİN!)

# CORS Configuration (Backend)
# ALLOWED_ORIGINS=                    # Opsiyonel: Backend CORS için izin verilen origin'ler
                                      # Boş bırakılırsa FRONTEND_PORT'tan otomatik oluşturulur (http://localhost:${FRONTEND_PORT})
                                      # Manuel belirtmek isterseniz: ALLOWED_ORIGINS=http://localhost:81

# FQDN Configuration
# FQDN=                               # Production için FQDN belirtin (örn: trivy.example.com)

# System Configuration
TZ=Europe/Istanbul                   # Timezone

# ============================================
# PRODUCTION (LB ile Kullanım)
# ============================================
# Production'a geçerken yukarıdaki değerleri aşağıdaki gibi değiştirin:

# FQDN=trivy.example.com              # Frontend domain'i (LB'den gelen DNS kaydı)
# VITE_API_BASE=https://trivy.example.com/api  # LB backend'i /api path'inde expose ediyorsa
# VITE_API_BASE=https://api-trivy.example.com   # VEYA LB backend'i ayrı subdomain'de expose ediyorsa
# ALLOWED_ORIGINS=https://trivy.example.com     # Backend CORS (birden fazla için virgülle ayırın)
